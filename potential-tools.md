- block tools vs canvas tools
- read tools vs write tools
- document level tools vs block level tools


1. A tool to let me know the dimensions of a block, like the number of characters, number of rows taken on the canvas, number of columns taken on the canvas, the length and height of the block, etc. (read tool, block level tool, block + canvas tool)
2. Update the content of the block (write tool, block level tool)
